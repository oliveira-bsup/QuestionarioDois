//Painel um
//function fotoAleatoria() {
//    const painelPrinc = document.querySelectorAll('img.painel');
//    let min = 1;
//    let max = 100;
//    painelPrinc.forEach(img => {
//        let numIntervalo = Math.floor(Math.random() * (max - min + 1)) + min;
//        img.src = `https://picsum.photos/id/${numIntervalo}/300/200?`;
//    });
//}
//setInterval(fotoAleatoria, 3000);

